export enum TRegistrationTypeEnum {
  GOOGLE = 'Google',
}

export enum TActorTypeEnum {
  USER = 'USER',
  DOCTOR = 'DOCTOR',
}

export enum TConsultationStatusEnum {
  SCHEDULED = 'SCHEDULED',
  IN_PROGRESS = 'IN_PROGRESS',
  COMPLETED = 'COMPLETED',
  CANCELLED = 'CANCELLED',
}
